namespace Troops.Base
{
    /// <summary>
    /// Enum for different troop states
    /// </summary>
    public enum TroopState
    {
        Idle,
        Moving,
        Attacking,
        Defending,
        Fleeing,
        Dead
    }
}