namespace Core.ECS
{
    /// <summary>
    /// Interface for all ECS components. Components are pure data containers.
    /// </summary>
    public interface IComponent
    {
        // Marker interface for all components
    }
}